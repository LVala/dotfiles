# fish_variables is cluttered with vars set by plugins
# and I prefer to do "set -g" in config.fish
fish_variables

# fisher installs plugins to these dirs by default
# and I keep all of my stuff in config.fish
completions/
conf.d/
functions/
themes/
